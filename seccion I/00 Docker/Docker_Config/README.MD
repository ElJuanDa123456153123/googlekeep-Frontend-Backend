
## Instrucciones de la instalacion
# ejecutar el siguiente comando en consola en la raiz donde se encuentra docker-compose.yml el comando descargara todos los archivos necesarios para iniciar el contenedor

> docker compose up -d  // con up levantamos el contenedor